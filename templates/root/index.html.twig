{% extends 'base.html.twig' %}

{% block body %}
<section class="hero is-fullheight-with-navbar">
    <div class="hero-body">
        <div class="container has-text-centered">
            <h1 class="title">Welcome to Aboprecis!</h1>
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <a class="button is-danger" href="{{ path('mysubs') }}">
                    <span class="icon">
                        <i class="fab fa-youtube"></i>
                    </span>
                    <span>My subs</span>
                </a>
            {% endif %}
        </div>
    </div>
</section>
{% endblock %}
